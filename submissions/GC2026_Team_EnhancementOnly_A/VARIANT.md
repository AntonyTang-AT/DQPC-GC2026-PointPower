# Submission variant: **A (full dependencies)**

**Recommended for organizers / paper reproduction.**

Includes all Python modules used by the refine pipeline, including optional temporal-refinement helpers (`enh_temporal*.py`) and `evaluate_uvg.py`.

Same runtime behaviour as variant B for the production preset `holefill_adaptive_frame_gate_v2` (frame_gate v2 hybrid).

See `../SUBMISSION_VARIANTS.md` for comparison with variant B and the preserved original snapshot.
